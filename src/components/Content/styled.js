import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  container: {
    width: '100%',
    height: '100vh',
  },
  paper: {
    padding: '10px',
    marginBottom: '5px',
    cursor: 'pointer',
  }
}));
